export interface InfoCardProps {
    title: string;
    message: string;
    link: string;
    image?: React.ReactNode;
    icon?: React.ReactNode;
    bgColor?: string;
    linkTitle?: string;
}